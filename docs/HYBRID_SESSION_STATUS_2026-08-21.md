# SportsEdge Hybrid Session Status — 2026-08-21

Integration state: `INTEGRATION_UNRUN`.

No feature PR was opened, no pending PR #96/#101-#108 was modified, no merge was performed, no model math/weight/threshold/promotion gate changed, and V6 remains parked.

## Execution distinction

- **CONNECTOR_EXACT_INSPECTION**: exact files/branches read from the private GitHub repository through the connected GitHub interface. Not an authenticated clone; not runtime seam evidence.
- **LOCAL_RECONSTRUCTED_PASS**: standalone research logic executed in a temporary local workspace. Useful unit evidence only.
- **BRANCH_FAITHFUL_INTEGRATION**: none in this session.

Consolidated local research test run:

```text
..................                                                       [100%]
18 passed in 0.25s
```

This combines:

- AutoRunReport AST guard: 3 tests
- V6 evidence-integrity adversarial harness: 6 tests
- Engine B LABELRULES/coverage harness: 9 tests

---

## Queue status matrix

| Item | Deliverable/result | PRESENT ON MAIN | RUNTIME EXECUTED | PRODUCING EVIDENCE |
|---|---|---|---|---|
| A1 | AutoRunReport constructor audit; known main/#96 positional seam; local AST guard | PARTIAL — risky production sites are on main; guard is not | LOCAL_RECONSTRUCTED_PASS 3/3; exact composed tree UNRUN | NO |
| A2 | MLB V7 + NFL/CFB M2 leakage inspection; no observed emitted-feature leak; CFB alias-guard gap | YES for inspected code | NO — connector inspection only | NO |
| A3 | Determinism audit; fixed-seed MLB path; football `seed=None` risk; wall-clock boundaries | YES for inspected code | NO — connector inspection only | NO |
| A4 | Import graph: archive stdlib-only; no pricing->simulation or funnel->pricing violation found in requested files | YES | NO — connector inspection only | NO |
| B1 | Divergent-topology Sept. 1 runbook | NO — docs branch only | NO — documentation | NO |
| B2 | Frozen `RUN IT` fixture spec incl. frozen market + irrelevant-environment variation | NO — docs branch only | NO — documentation | NO |
| C1 | V6 chronology/witness integrity checker prototype | NO | LOCAL_RECONSTRUCTED_PASS as part of 6/6 | NO |
| C2 | Adversarial zero-contribution tests | NO | LOCAL_RECONSTRUCTED_PASS 6/6 suite | NO |
| C3 | Provenance/boundary spec + GitHub witness semantic correction | NO — docs branch only | NO production run; source semantics verified | NO |
| D1 | Retrosheet field feasibility by seven labels | NO — docs branch only | NO Retrosheet dataset run | NO |
| D2 | Deterministic `LABELRULES_V1` local prototype | NO | LOCAL_RECONSTRUCTED_PASS 9/9 suite | NO |
| D3 | Cause-label coverage harness | NO | LOCAL_RECONSTRUCTED_PASS within 9/9 | NO historical coverage evidence |
| D4 | Frozen seasons/version-stamp harness `[2021..2025]` | NO | LOCAL_RECONSTRUCTED_PASS within 9/9 | NO |
| E | Evidence-readiness backlog across all MLB market families | NO — docs branch only | NO — documentation/audit ranking | NO |
| F1 | Separate `manual_analysis` research ledger; no model backfill | NO — research branch only | NO pipeline run | NO by contract |
| F2 | Schedule-relative heartbeat refinement | NO — docs branch only; #101 untouched | NO; detection/delivery acceptance UNRUN | NO |
| G1 | Football event-generated key-number baseline spec | NO — docs branch only | NO | NO |
| G2 | Legacy score-generator deprecation spec | PARTIAL — legacy generator exists; target spec not on main | NO | NO |

---

## A1 detailed blocker

Production conversion of every `AutoRunReport` call to keywords is deliberately **BLOCKED BY FREEZE**. The research session prepared and executed a local AST guard, but installing it or changing #96 would violate the freeze.

Exact unblock:

1. Sept. 1 infrastructure restored.
2. Freeze one composition base SHA.
3. Compose #96 changes onto that tree.
4. Convert surviving constructor sites to keywords in that composition.
5. Run the AST guard against the exact tree.
6. Run imports/unit/seam tests branch-faithfully.

The private connector code-search index also returned no matches even for known symbols. Exhaustive all-file/all-branch constructor enumeration therefore requires an authenticated clone or restored code index.

## A2 detailed blocker

The exact inspected M2/V7 files show no emitted market-derived feature contamination. However the requested **repo-wide grep with every hit and exact file:line** is not honestly complete because private code search failed and this session does not have an authenticated clone.

Exact unblock: clone the frozen SHA, then run recursive grep/AST scanning for line/total/price/odds/implied/closing/book aliases through feature builders/configs and inspect every hit.

## A3 detailed blocker

Known exact risks are documented, but "every RNG/wall-clock/order/filesystem dependency" requires a real cloned tree for static scanning.

Exact unblock: clone frozen SHA and scan for `random`, NumPy RNG constructors, `datetime.now`, `utcnow`, `time.time`, unordered `os.listdir`/glob/set/dict-derived serialization, temporary-path/process-ID leakage, and environment-variable reads reaching canonical artifacts.

## A4 detailed blocker

Requested critical boundaries were inspected and no prohibited coupling was found. Full import-DAG proof still requires an AST/import graph over a real clone.

## B1/B2 blockers

Documentation is complete. Execution is intentionally blocked until Sept. 1. Archive restart remains first because missed capture windows are unrecoverable.

## C1-C3 blockers

The local prototype is not connected to V6 production code. Additionally, GitHub source verification found that the current Enterprise Cloud REST Events API `PushEvent` does not document a `commits[]` array while push webhooks do. The integrity spec now prefers a versioned push-webhook delivery witness that proves both remote time and exact commit membership.

Exact unblock:

- establish/persist the chosen witness source,
- compose integrity code onto the exact frozen production tree,
- run adversarial tests branch-faithfully,
- only then allow restored forward observations to begin at observation #1.

Historical 53 observations remain separate; outage gap is never reconstructed.

## D1-D4 blockers

The labeling machinery is locally deterministic but has **zero historical cause evidence**.

Exact unblock:

1. acquire/freeze Retrosheet 2021-2025 source files and hashes,
2. build ex-ante pregame workload prior/starter classes,
3. parse only source-supported fields,
4. execute `LABELRULES_V1`,
5. report actual `cause_label_coverage`,
6. keep unclassifiable injury/tactical/weather cases `UNKNOWN`,
7. only then assess distribution, threshold and mechanism out of time.

## E blocker discipline

No research idea is fitted merely because it is listed. Tier 2 calibration remains blocked until forward archive evidence exists; market-family fitting requires `n >= 300`, otherwise `INSUFFICIENT_EVIDENCE`.

## F1 blocker discipline

The old 1-6 summary is not expanded into synthetic tickets. SEA/HOU NRFI relationship to that aggregate remains unresolved. Current manual rows leave unknown stake/timestamp/result fields blank rather than guessing. Manual rows never enter Brier/log-loss/promotion/first pipeline CLV.

## F2 blockers

Two independent acceptance gates remain UNRUN:

- heartbeat detects the existing stale obligation,
- the alert actually reaches the user.

Archive 3-hour freshness is now schedule-relative: only an actual capture opportunity creates an obligation. Overnight age alone is not a failure.

## G blockers

The verified main football simulator is score-level and injects key-number mass; an event-rich drive/play production generator is not established in the verified state. Therefore deprecation contract tests cannot honestly execute yet.

Exact unblock: event-rich generator present + deterministic fixtures + historical `[2021,2022,2023,2024,2025]` key-number comparison + production source-identity wiring test.

---

## Things deliberately not fudged

1. **Private code search failure:** did not claim exhaustive A1/A2/A3 coverage or invent file:line results.
2. **Rule 2/Rule 3 ambiguity:** first Engine B implementation exposed it with failing tests; the research taxonomy now explicitly numbers labels in listed order instead of silently choosing precedence.
3. **GitHub witness mismatch:** did not assume REST Events `PushEvent` has a commit list; corrected the spec to a witness source that can prove membership.
4. **Retrosheet mechanism limits:** did not infer injury/manager motive/weather cause from generic game context.
5. **Manual bet history:** did not backfill Model_P/version/hash or manufacture seven rows from a 1-6 aggregate.
6. **Determinism target:** did not require volatile run timestamps to be byte-identical; canonical decision bytes are the target.
7. **Integration:** did not treat component/local test success as integration evidence.
8. **V6:** did not unpark or iterate it toward the gate.

## Real clone vs reconstructed

- Authenticated real clone used: **NO**.
- Exact private GitHub source inspected through connector: **YES**.
- Standalone reconstructed local code executed: **YES**.
- Exact divergent composed branch tree executed: **NO**.

This distinction is load-bearing: reconstructed tests can smooth over the import/signature seams that Sept. 1 is intended to discover.

## Next unblocked item

Within the hard freeze, the ordered queue is complete at the research/design/local-prototype level. The next **integration** action is blocked until infrastructure returns and is already specified by the Sept. 1 runbook: baseline proof-of-life, archive restart/in-window acceptance first, then frozen-base composition and branch-faithful execution.

Until then, only additional research/backlog/source documentation is unblocked; none may silently enter model code.

`INTEGRATION_UNRUN`
