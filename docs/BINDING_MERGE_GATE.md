# Binding merge gate

Before merge: run full repository tests plus the new binding attacks, inspect failures for compatibility fallout, and confirm `config/deployments.json` eligibility and frozen edge floors are unchanged. Do not merge merely because the new focused tests pass.
