# Readout requirements

The probability/readout side must explicitly identify market, source market, game, entity, side, period, threshold when thresholded, and team when team-bound. These fields describe the probability that was actually computed. They may originate from the model request contract, but must not be copied from a sportsbook quote after computation to make a mismatch disappear.
