# No-synthesis rule

If `event_id`, `game_number`, `book_key`, canonical team/player identity, executable price, or retrieval timestamp is absent from the normalized offer, binding fails. The system must not fill the field from the MLB schedule, model input, feature row, paired quote, or another sportsbook row merely to satisfy the validator. Reconciliation belongs in the acquisition adapter with auditable source evidence.
