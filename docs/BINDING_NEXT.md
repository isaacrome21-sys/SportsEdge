Next integration target: acquisition adapters. Populate trusted quote-origin `event_id`/`game_number`, canonical `team_id`, player/pitcher IDs, `book_key`, price, and retrieval timestamp at normalization. Do not derive these from model output. After adapter coverage, update model readouts to emit explicit probability binding identity and run end-to-end 38-market omission attacks before any PASS count is raised.
