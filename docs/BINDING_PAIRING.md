# Paired quote binding

Both candidate and opposite quotes must independently pass canonical quote and binding-origin validation before no-vig pairing. Pair identity must include canonical event instance, period, market, entity/team where applicable, threshold, book, and alternate status. Missing timestamps are never treated as fresh by default.
