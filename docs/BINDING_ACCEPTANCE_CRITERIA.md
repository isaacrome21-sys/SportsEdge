# MLB binding acceptance criteria

A market earns binding PASS only when a real normalized offer supplies all quote-origin identity, the matching model readout supplies all probability-origin identity, omission/mismatch attacks fail closed, paired quotes bind to the same event/book/entity/threshold, and the row traverses the orchestration gate end-to-end. A unit-only spec declaration is not PASS.
