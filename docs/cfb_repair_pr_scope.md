# CFB repair PR scope

This change set is deliberately pre-attempt. It repairs the candidate-preregistration refreeze path, restores the guarded reconstructed-selection materializer, and adds a single train/freeze control entrypoint that proves provider readiness but refuses to consume the first candidate evaluation until the selected-candidate serving contract is implemented.

Nothing in this branch may be interpreted as promotion evidence or betting authority. All SportsEdge Truth Gate, promotion, eligibility, staking, evidence-clock, backfill, and OFFICIAL boundaries remain unchanged.
