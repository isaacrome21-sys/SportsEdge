# CI

Run the repository's normal full test workflow. For a focused local check, discover the new tests with Python unittest/pytest according to the repository's existing test runner. The merge decision must use the full suite, not only focused binding tests.
