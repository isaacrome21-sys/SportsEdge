# Research Bet Ledger

Rows stored here use the `manual_analysis` source class defined in `docs/BET_LEDGER_V1.md`.

These records are intentionally isolated from pipeline calibration/promotion evidence. Do not backfill model probabilities or model hashes that were not captured at bet time.
