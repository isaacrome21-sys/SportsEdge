name: rebuild-pitcher-bb-v4

on:
  push:
    branches: [full-slate-market-completion-20260811]
    paths:
      - sportsedge/historical_cutoff.py
      - scripts/rebuild_pitcher_bb_v4.py
      - tests/test_historical_cutoff.py
      - tests/test_bb_cutoff_chronology.py
      - audit/validation_events.jsonl
      - .github/workflows/rebuild-pitcher-bb-v4.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rebuild:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install model-build dependencies
        run: python -m pip install --disable-pip-version-check numpy scikit-learn joblib pytest
      - name: Verify BB cutoff chronology tests
        run: python -m pytest -q tests/test_historical_cutoff.py tests/test_bb_cutoff_chronology.py
      - name: Restore BB historical cache
        uses: actions/cache@v4
        with:
          path: .cache/sportsedge/bb-rebuild
          key: sportsedge-bb-rebuild-v4-${{ runner.os }}
          restore-keys: |
            sportsedge-bb-rebuild-v3-${{ runner.os }}
      - name: Rebuild cutoff-correct pitcher BB with untouched 2026 holdout
        run: python scripts/rebuild_pitcher_bb_v4.py
      - name: Upload BB cutoff-correct validation evidence and frozen candidate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pitcher-bb-v4-cutoff-correct
          path: |
            artifacts/pitcher_bb_v4_validation.json
            artifacts/sportsedge_pitcher_bb_v4.joblib
          if-no-files-found: warn
          retention-days: 14
